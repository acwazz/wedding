[project]
name = "wedding-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.141.1",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "workers-py",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
